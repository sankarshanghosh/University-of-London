Perlin noise was originally developed by
a computer scientist called Ken Perlin in the early 80s in order to
procedurally generate textures. What does that even mean? Say I wanted to imitate
the look of wood or marble. I could hire an artist to use
a digital brush and paint a texture or I could use an algorithm that
generates that texture procedurally. The latter of course being a much
cheaper and faster option. That's the reason it was developed and
it was used almost immediately to generate more realistic procedural textures in
the original Tron movie from 1982. Here are some more intricate examples of
3D textures that look very realistic and very plausible. But were actually generated
procedurally using Perlin noise. Perlin noise can do quite
diverse things too. In the image of the mountain range, we can use it to sculpt the 3D
surface of the mountains. To decided where snow should be placed and
where the water should reach and we could even use it to generate
the cloud patterns in the sky. So here, for example, you see
a work that I did a few years back. I program some interactive projections
while using Perlin noise to do that movement of the tree that you see being
moved left and right pretending that it is pushed by the wind and
the leaves that are falling off of it and the movement of the leaves in the air,
all that is controlled by Perlin noise. So you don't use Perlin noise only for
the colors of the textures. You can also use it for
things like movement. We've already seen what random can do for
us and what its limitations are. The reason we'll be using Perlin from now
on is because it allows for a more organic appearance and it produces a naturally
ordered sequence of pseudo random numbers. See, for example, the top graph here
is a random numbers generated by P5 and plotted as a graph. Each time I asked P5 for
a new random number, it does not take into account
what has happened before. It generates a new number
with no sense of history. That is why you can be all the way
at the top at one time and drop all the way the bottom
the next request. The graph at the bottom shows
Perlin noise over time, notice how much smoother the curve is. What this demonstrates is that each value
generated is taking into account what happened exactly before it lets
go see that on the Whiteboard. Imagine that the values we get
out of noise are the temperatures in a city over a period of the week. We ask it, what is the temperature? Let's say on Monday at 9AM. And noise says it's 15 degrees. if we ask it a minute later what the
temperature is or let's say an hour later, what the temperature is, it will say
maybe it went up to 15.5 degrees. If I ask it three hours later,
maybe then the temperature is 17 degrees. But you see small steps in time account
make for small differences in temperature. If I make a big step and I say what is
the temperature say on Friday at 9AM. Maybe then the temperature is 40 degrees,
a big step in time makes for a big difference in temperature and
possibly. But that is the sort of
relationship that the values of a noise have with each other
just like the weather temperature. If on the other hand you compare
it with a random function and let's say this is a simulation
of the random values plotted, I ask here what is the temperature,
the system says 40 degrees. I asked it after one minute and
it says maybe 10 degrees. After one minute. That's not how the weather works. The weather is dependent, the temperature
now is linked to the temperature that was one minute ago and will be linked
to the temperature a minute later. Another way to think of noise
is with this piece of paper. Say I take this piece of paper and
I crumble it. I unfold it. And you see it from profile. Imagine that the noise function internally
describe what is crumbling looks like? Then when I call it, all I ask it for is to give me the height of
the paper at a specific point. The noise function behave like a record
player where the needle reads the crumpled piece of paper at
a specific point in time. See how I move the needle and
I'm reading the amount of crumbling, the amount the height of
the paper at a specific point. The nose function has
encoded this crumbling and the paper itself is a little
bit like a noise generator. It has a natural flow of numbers,
has a natural flow of height values. Let's see this in a live coding example,
and hopefully it will make a lot of sense. All right, so
let's code our first noise example. So I have here an empty program. Let's actually add refresh
the background at every frame. And what I want to do is store in nX, noise X the value of noise. So I'm going to create a global variable
here called time, var time = 0. That's the thing that is going
to change in every frame. So I'm going to say here
time = time + 0.001. Tiny little step. So noise X, so noise is taking this time. It starts in the beginning at 0 but it
doesn't have to be any point in the graph so I could start it let's say at 100,
1000. So noise, 1000 and then we know that
noise returns a value from 0 to 1. So if we're going to use nX as
a parameter, I need to map it. So I'll say here that var X locX = map(nx). So it takes the noise value that
we just generated which we know ranges from 0 to 1 and
then we'll map it to 0 to width. We scaling it effectively. And let's now use it to draw a rectangle. So I'll translate. Translate(locX, height/2) I'll put
it in the middle of the screen and let me just do something a little
thing here where we say the rectMode. So rectMode(CENTER) so that the rectangle
that I'm going to draw is going to be bang on, like it's going to be
defined by its central point. So I'm translating now in
the middle of the screen and I'm using the location X
data generated from noise. And let's draw the rectangle. So rectangle(0, 0 let's make it 50, 50 really big 100, 100. I run this up it's been done. I haven't filled it with something. Let's fill it with a color, fill(255). Still nothing. Okay, no panic, opening here the Tools,
Developer tools. An error. So what is it line 18 I go to the code. Rectangle(0, 0) what is it? Rectangle is not defined. I'm sorry. So the rectangle is rect. And there we go. We have a little rect
that is moving in time using the noise function probably
moving a little bit too slow. So I'm going to speed up how fast
I'm traveling down the nose curve. Okay, and now it's moving like this. So notice that the shape is centered
around the center of the screen. So that's where
the distribution of values, the majority of the of the values
of the distribution lie. If I instead used here, random(width). The values could be anywhere,
in one frame when I use random in one frame the square might be
the beginning of the canvas, in the next frame it might
be the end of the canvas. It's a completely different
behavior completely different results that I'm getting in its frame. It's just like when you throw
the ball in the roulette, it might be a 12-1 point the next time
you throw the ball in the roulette doesn't care about its history,
but noise didn't behave like that. It's noise. Putting it back here. Noise produces this organic movement
of this square as it moves left and right on the screen. So let's add also bit of rotation. because I can do anything
I want with this. I can take this value and next that is
generated here and I can do what I want. Actually before I do that let's
add a little bit of color. So I can say var g, G for
green map(nX) the value that I got for the which I know again is from 0 to 1 and
I want you to map it from 0 to 255. So here in the fill,
I could say fill(0, g, 0). So now I'm producing
a little green square. Now notice that the green becomes dark
when the square goes to the left and it becomes brighter when it goes
to the right always in this case. That's because I have coupled
the movement with the color. They're using both the same
source both the green and the movement loc X is using nX. So they are coupled with each other. Let's couple also rotation to it. So I could say var rotZ = map(nx, 0, 1) and let's do a rotation from negative 25 to 25. And here I'll do rotat( rotZ). Oops, 916, 916 what's the error? References and next is not defined. There we go, nx. So again, look at the rotation, the rotation is also coupled
with the location X. When it goes to the right it
spins in a certain way and when it goes to the left it spins
the other way, they are not independent. Color, location, and
rotation are all coupled together because they
are depending on this value nX. So, I'll make this one final
example even more explicit. Let's add var locY and use again map(nX, 0, 1, 0 height) and I'll use this value in here. I wonder if you can guess what
will happen now, what we will see. So we see diagonal movement. That's because again,
I coupled the location Y is coupled with location X because
both of them are using nX. So whatever X is Y pretty much is. They're not exactly the same because one,
the width and the height are not exactly the same but
the ratio is the same. So that's why this object
is moving diagonally. So this should give you an idea like if
I want to have now this rectangle move independently on the x-axis
from the y-axis. I want to decouple these things,
the sources of the noise value. So let's create here another var nY for
noise Y, equals noise and instead of saying time which would have produced
exactly the same results as before. So if I just use time
again the same variable, I should you see the same result,
the diagonal movement. So if I want to decouple them, I'll go slightly later
in the noise function. So I'll say here maybe let's say plus
5 doesn't really matter what value I put there as long as it's late enough. So now the object is moving on
the x-axis independently for how it moves on the white axis. Let's see this on the Whiteboard. So, before I was getting the same value
for nX and nY, I was getting that value. So, if now I'm the decoupling them,
I'm getting nX from here. We're getting the value of nX from here so
I'm getting a noise value that I'm going to use for movement on the X
axis from that point in the graph and I'm picking another point in the graph. So this is let's say we call this time and then I'm picking another point
in the graph say this one. And this is time plus,
I add in the the code that said plus 5. So whatever my unit is if this is 5,
I went a little bit later in time so that I'm reading different
points on the graph. So as one might go up and
down the other one is going up and down in a different way
because they are decoupled, they are reading a different
point in the noise graph. The color and the rotation at the moment
is still reading the same point. So, we have now this type of movement
with this tiny little step in time reading the graph and
we're reading it with a certain speed. If I make the speed slower if
I move thousands every time you see a slower speed because
I'm reading the graph, the noise graph much slower and
everything moves slower because everything changes
a lot less from frame to frame. You get more of more similar values. If I make a big step,
let's say time plus 1, 1 is a huge value in that noise graph. What you'll see is you'll
see something that might look to you in
the beginning like it's random. Like one value here, one value there. So think about the weather example. At this point it's like asking
what the temperature today, what's the temperature in four months, what's the temperature in three months and
so on and so on and so on. The steps that will make in are so big that it could be raining today
sunny tomorrow and they look random. In fact, again, notice that it's
a little bit different from random because random would spread
the values anywhere on the canvas. Here there's still a tendency for them to
be in the center and to deviate from that. So let's go back to small values and more saying representation,
more saying behavior for our object. In this example, we show how to use noise
and that by combining it with the map function we can translate the numbers
produced to organic movement and color. In reality since everything with do
with P5 is described in numbers, anything you can think of
can be controlled by noise.